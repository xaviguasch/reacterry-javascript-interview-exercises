// String.toUpperCase
// In this coding challenge, you will be implementing a function called getUpperCase(word) that takes one argument: a word. The function should return the word converted to UPPERCASE.

// Directions
// If the input word is empty, then return an empty string.

const getUpperCase = (word) => {
  // Write your code here
  return word.toUpperCase()
}

console.log(getUpperCase('hello'))
console.log(getUpperCase('WoRlD'))
console.log(getUpperCase('tEsT'))
