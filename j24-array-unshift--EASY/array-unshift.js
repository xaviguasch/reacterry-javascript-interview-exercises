// Array.unshift
// In this coding challenge, you will be implementing a function called addFromBeginning(array, value) that takes two arguments: an array and a value. The function should add value to the array from the beginning.

const addFromBeginning = (array, value) => {
  // Write your code here
  return
}

console.log(addFromBeginning([1, 2, 3], 0))
console.log(addFromBeginning(['foo', 'bar'], 'baz'))
