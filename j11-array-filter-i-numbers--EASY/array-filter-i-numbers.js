// Array.filter I - numbers
// Implement a function called getLessThanFive that takes an array of numbers as input and returns a new array containing only the elements that are strictly less than 5.

// Directions
// If no elements in the input array are less than 5, return an empty array.

const getLessThanFive = (numbers) => {
  return
}

console.log(getLessThanFive([2, 6, 3, 7, 1, 9, 4]))
