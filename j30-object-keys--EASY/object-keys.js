// Object.keys
// In this coding challenge, you will be implementing a function called getKeys(object) that takes an argument: an object. The function should return an array will all of the object’s keys.

// Directions
// If the object has no keys, return an empty array.

const getKeys = (object) => {
  return
}

const object = { foo: 1, bar: 2, baz: 3 }
console.log(getKeys(object)) // Output: ['foo', 'bar', 'baz']
const object2 = { foo: 1, bar: 2 }
console.log(getKeys(object2)) // Output: ['foo', 'bar']

const object3 = {}
console.log(getKeys(object3)) // Output: []
