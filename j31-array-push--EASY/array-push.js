// Array.push
// In this coding challenge, you will be implementing a function called append(array, value) that takes two arguments: an array and a value. The function should append the value to the end of the array.

const append = (array, value) => {
  // Write your code here
}

console.log(append([1, 2, 3], 4))
console.log(append(['foo', 'bar'], 'baz'))
