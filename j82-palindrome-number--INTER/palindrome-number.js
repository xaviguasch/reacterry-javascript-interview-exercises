// Palindrome number
// Write a function isPalindrome(number) that takes an integer number and returns true if the number is a palindrome and false otherwise.

// A palindrome is a word, phrase, number, or another sequence of characters that reads the same forward and backwards. For example, "racecar" is a palindrome because it reads the same way forwards and backwards.

// Directions
// You can assume that the input number will always be an integer.

const isPalindrome = (number) => {
  // Write your code here
}

console.log(isPalindrome(123321)) // Output: true
console.log(isPalindrome(12321)) // Output: true
console.log(isPalindrome(123)) // Output: false
