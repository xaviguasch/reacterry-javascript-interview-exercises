// Longest word in a sentence
// In this coding challenge, you will be implementing a function findLongestWord(sentence) that takes a string sentence as its argument and returns the longest word in it. If there are two or more words of the same length, then the function should return the word whose’ first occurrence has a smaller index.

// Directions
// If no such word is found then return null.
// You can assume that the sentence is always a valid and properly formatted human sentence where words are separated by just one whitespace.

const findLongestWord = (sentence) => {
  // Write your code here
}

const sentence = 'Welcome to reacterry'
console.log(findLongestWord(sentence)) // Output: 'reacterry'
